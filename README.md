## Unify Tracker chrome extension & middleware

### install chrome extension

first load unpacked chrome extension by set path to `[path-to-this-root-directory]/chrome-ext`

### run middleware

`node server.js`
